import type { SxProps, Theme } from "@mui/material/styles";

export const contractFooterRow: SxProps<Theme> = {
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  gap: 1.5,
  flexWrap: "wrap",
  mt: 2,
};
