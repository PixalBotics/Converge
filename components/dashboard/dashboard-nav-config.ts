/** @deprecated Keep imports on `@/lib/permissions` (single source). */
export {
  DASHBOARD_NAV_ITEMS,
  getVisibleDashboardNavItems,
  isNavPathSelected,
  type DashboardNavItem as ActivityNavRow,
  type DashboardSidebarIconKey,
} from "@/lib/permissions";
