"use client";

import { EmailPlaceholderPage } from "@/features/email";

export default function Page() {
  return (
    <EmailPlaceholderPage
      title="Custom email forms"
      description="Per child company and website overrides (API next)."
    />
  );
}
