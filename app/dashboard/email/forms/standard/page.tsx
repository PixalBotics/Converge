"use client";

import { EmailPlaceholderPage } from "@/features/email";

export default function Page() {
  return (
    <EmailPlaceholderPage
      title="Standard wrap-up form"
      description="Per child company + website — stores default visitor/wrap-up fields (API next)."
    />
  );
}
